{
  "name": "pop-subgraph",
  "license": "UNLICENSED",
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "build:arbitrum-one": "cp subgraph.yaml .subgraph.yaml.orig && graph build --network arbitrum-one; s=$?; mv -f .subgraph.yaml.orig subgraph.yaml; exit $s",
    "build:gnosis": "cp subgraph.yaml .subgraph.yaml.orig && graph build --network gnosis; s=$?; mv -f .subgraph.yaml.orig subgraph.yaml; exit $s",
    "deploy:arbitrum-one": "cp subgraph.yaml .subgraph.yaml.orig && graph deploy --network arbitrum-one --node https://api.studio.thegraph.com/deploy/ poa-arb-v-1; s=$?; mv -f .subgraph.yaml.orig subgraph.yaml; exit $s",
    "deploy:gnosis": "cp subgraph.yaml .subgraph.yaml.orig && graph deploy --network gnosis --node https://api.studio.thegraph.com/deploy/ poa-gnosis-v-1; s=$?; mv -f .subgraph.yaml.orig subgraph.yaml; exit $s",
    "create-local": "graph create --node http://localhost:8020/ poa-arb-v-1",
    "remove-local": "graph remove --node http://localhost:8020/ poa-arb-v-1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 poa-arb-v-1",
    "test": "graph test --version 0.6.0"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
